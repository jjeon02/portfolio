### `MEMO FOR MYSELF`

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.


### `npm run deploy`
Edit code on main
Commit your changes
Run npm run deploy → it builds and updates the live site automatically